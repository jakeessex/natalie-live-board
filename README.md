# Natalie live board

Private password-gated call board for Natalie (Jake Essex Music Ltd).

Live: https://jakeessex.co.uk/natalie-live-board/

Password stays `natbooksjake`. Gate and `noindex` stay. This is not the public marketing site.

Board v18 — default view All (working list). Filters: All · Close · Replied · Call · Locked · Parked.
Close is only venues Nat can lock a date/fee on this phone call. Committee / away / “I’ll send dates” sit in Live or Chase.
Cash always uses the fee on the thread. Stored `lockedFee` is fallback only. Bromley £400 lump. East Barnet / Tina pending until a date is locked.
Nat calls — no Email or Text buttons. Maps + Web on every card. Header scrolls; filters sit at the bottom.
